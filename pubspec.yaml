name: dart_pieces_mcp
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.1

dev_dependencies:
  http: ^1.1.0
  lints: ^5.0.0
  test: ^1.24.0

dependencies:
  fetch_client: ^1.1.2
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sse: ^4.1.7
